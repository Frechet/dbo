spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:tc:postgresql:16.4:///dbo
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create

app:
  deposit:
    scheduler:
      interval: 30000